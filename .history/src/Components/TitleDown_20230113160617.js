import React from "react";
import styles from "./TitleDown.module.scss";

const TitleDown = () => {
	return (
		<div className={styles.container}>
			<h1>
				Loved our quote? Why not buy something with it in it?
			</h1>
			<<button>
    <span class="box">
        Hover!
    </span>
</button>
		</div>
	);
};

export default TitleDown;
